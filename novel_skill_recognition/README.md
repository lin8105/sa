# Novel-Skill Recognition

This directory is organized by recognition approach. The current method is [`r2/`](r2/), a contrastive temporal representation with local OLD-memory support. Future approaches may be added as separate method folders, for example `ssim/` or other baselines; no method-specific implementation is shared at this parent level.
